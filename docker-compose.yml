# application
website:
  build: .
  ports:
    - "80:80"
    - "443:443"
  links:
    - mysql:db
  environment:
    - DB_HOST=db

# third-party
mysql:
  image: mysql:5.5
  ports:
    - "3306:3306" # to connect with an SQL client
  environment:
    - MYSQL_ROOT_PASSWORD=mysqlpass
    - MYSQL_USER=blog_usr
    - MYSQL_PASSWORD=vj5pVr8Lg0B6C8Hb
    - MYSQL_DATABASE=blog